{"pr_number": 3741, "pr_title": "NXP-28417: S3 Glacier restore and blob status (10.10)", "pr_author": "efge", "pr_createdAt": "2020-02-11T20:23:22Z", "pr_url": "https://github.com/nuxeo/nuxeo/pull/3741", "timeline": [{"id": "MDI0OlB1bGxSZXF1ZXN0UmV2aWV3Q29tbWVudDM3OTMxMjMxNw==", "url": "https://github.com/nuxeo/nuxeo/pull/3741#discussion_r379312317", "body": "throws `IOException` is not needed  ", "bodyText": "throws IOException is not needed", "bodyHTML": "<p dir=\"auto\">throws <code>IOException</code> is not needed</p>", "author": "RSalem07", "createdAt": "2020-02-14T08:53:51Z", "path": "addons/nuxeo-core-binarymanager-cloud/nuxeo-core-binarymanager-s3/src/main/java/org/nuxeo/ecm/blob/s3/S3BlobProvider.java", "diffHunk": "@@ -179,4 +189,45 @@ protected String getContentDispositionHeader(Blob blob) {\n         return RFC2231.encodeContentDisposition(blob.getFilename(), false, null);\n     }\n \n+    @Override\n+    public BlobStatus getStatus(ManagedBlob blob) throws IOException {", "originalCommit": "55b02cbd4f5bd050b6f645ef138b36d5ad5bb0c2", "replyToReviewId": null, "replies": [{"id": "MDI0OlB1bGxSZXF1ZXN0UmV2aWV3Q29tbWVudDM3OTU0NjA2NQ==", "url": "https://github.com/nuxeo/nuxeo/pull/3741#discussion_r379546065", "bodyText": "Actually it should have been, I intended to wrap any AmazonServiceException into an IOException. I'll change that.", "author": "efge", "createdAt": "2020-02-14T17:10:01Z", "replyToReviewId": "MDI0OlB1bGxSZXF1ZXN0UmV2aWV3Q29tbWVudDM3OTMxMjMxNw=="}], "type": "inlineReview"}, {"id": "MDI0OlB1bGxSZXF1ZXN0UmV2aWV3Q29tbWVudDM3OTMxMjcwOQ==", "url": "https://github.com/nuxeo/nuxeo/pull/3741#discussion_r379312709", "body": "you can use the constructor with three args (` public GetObjectMetadataRequest(String bucketName, String key, String versionId) ` ", "bodyText": "you can use the constructor with three args (public GetObjectMetadataRequest(String bucketName, String key, String versionId)", "bodyHTML": "<p dir=\"auto\">you can use the constructor with three args (<code>public GetObjectMetadataRequest(String bucketName, String key, String versionId)</code></p>", "author": "RSalem07", "createdAt": "2020-02-14T08:54:45Z", "path": "addons/nuxeo-core-binarymanager-cloud/nuxeo-core-binarymanager-s3/src/main/java/org/nuxeo/ecm/blob/s3/S3BlobProvider.java", "diffHunk": "@@ -179,4 +189,45 @@ protected String getContentDispositionHeader(Blob blob) {\n         return RFC2231.encodeContentDisposition(blob.getFilename(), false, null);\n     }\n \n+    @Override\n+    public BlobStatus getStatus(ManagedBlob blob) throws IOException {\n+        String key = stripBlobKeyPrefix(blob.getKey());\n+        String objectKey;\n+        String versionId;\n+        int seppos = key.indexOf(VER_SEP);\n+        if (seppos < 0) {\n+            objectKey = key;\n+            versionId = null;\n+        } else {\n+            objectKey = key.substring(0, seppos);\n+            versionId = key.substring(seppos + 1);\n+        }\n+        String bucketKey = config.bucketPrefix + objectKey;\n+        GetObjectMetadataRequest request = new GetObjectMetadataRequest(config.bucketName, bucketKey);\n+        request.setVersionId(versionId);", "originalCommit": "55b02cbd4f5bd050b6f645ef138b36d5ad5bb0c2", "replyToReviewId": null, "replies": null, "type": "inlineReview"}, {"id": "MDI0OlB1bGxSZXF1ZXN0UmV2aWV3Q29tbWVudDM3OTMxNDEzMA==", "url": "https://github.com/nuxeo/nuxeo/pull/3741#discussion_r379314130", "body": "wdyt if we replace it by:\r\n```\r\n        String objectKey = key;\r\n        String versionId = null;\r\n        int seppos = key.indexOf(VER_SEP);\r\n        if(seppos >= 0) {\r\n            objectKey = key.substring(0, seppos);\r\n            versionId = key.substring(seppos + 1);\r\n        }\r\n```", "bodyText": "wdyt if we replace it by:\n        String objectKey = key;\n        String versionId = null;\n        int seppos = key.indexOf(VER_SEP);\n        if(seppos >= 0) {\n            objectKey = key.substring(0, seppos);\n            versionId = key.substring(seppos + 1);\n        }", "bodyHTML": "<p dir=\"auto\">wdyt if we replace it by:</p>\n<div class=\"snippet-clipboard-content position-relative overflow-auto\" data-snippet-clipboard-copy-content=\"        String objectKey = key;\n        String versionId = null;\n        int seppos = key.indexOf(VER_SEP);\n        if(seppos &gt;= 0) {\n            objectKey = key.substring(0, seppos);\n            versionId = key.substring(seppos + 1);\n        }\"><pre><code>        String objectKey = key;\n        String versionId = null;\n        int seppos = key.indexOf(VER_SEP);\n        if(seppos &gt;= 0) {\n            objectKey = key.substring(0, seppos);\n            versionId = key.substring(seppos + 1);\n        }\n</code></pre></div>", "author": "RSalem07", "createdAt": "2020-02-14T08:58:05Z", "path": "addons/nuxeo-core-binarymanager-cloud/nuxeo-core-binarymanager-s3/src/main/java/org/nuxeo/ecm/blob/s3/S3BlobProvider.java", "diffHunk": "@@ -179,4 +189,45 @@ protected String getContentDispositionHeader(Blob blob) {\n         return RFC2231.encodeContentDisposition(blob.getFilename(), false, null);\n     }\n \n+    @Override\n+    public BlobStatus getStatus(ManagedBlob blob) throws IOException {\n+        String key = stripBlobKeyPrefix(blob.getKey());\n+        String objectKey;\n+        String versionId;\n+        int seppos = key.indexOf(VER_SEP);\n+        if (seppos < 0) {\n+            objectKey = key;\n+            versionId = null;\n+        } else {", "originalCommit": "55b02cbd4f5bd050b6f645ef138b36d5ad5bb0c2", "replyToReviewId": null, "replies": [{"id": "MDI0OlB1bGxSZXF1ZXN0UmV2aWV3Q29tbWVudDM3OTU0NzMwNQ==", "url": "https://github.com/nuxeo/nuxeo/pull/3741#discussion_r379547305", "bodyText": "I prefer the \"static single assignment\" form, where variables are assigned only once if possible and if it doesn't make the code unreadable.\nNote that this pattern helps lambdas as well (not in this case, there's no lambda) as it makes the variables effectively-final.", "author": "efge", "createdAt": "2020-02-14T17:12:45Z", "replyToReviewId": "MDI0OlB1bGxSZXF1ZXN0UmV2aWV3Q29tbWVudDM3OTMxNDEzMA=="}], "type": "inlineReview"}, {"id": "MDI0OlB1bGxSZXF1ZXN0UmV2aWV3Q29tbWVudDM3OTMxNzYyNg==", "url": "https://github.com/nuxeo/nuxeo/pull/3741#discussion_r379317626", "body": "i checkout you branch and i get an error compilation here, pehaps i miss something, but your static import \r\n`import static org.apache.commons.lang.StringUtils.defaultIfEmpty;` doesn't exist i replace it by `import static org.apache.commons.lang3.StringUtils.defaultIfEmpty;` i think we use apache common 3", "bodyText": "i checkout you branch and i get an error compilation here, pehaps i miss something, but your static import\nimport static org.apache.commons.lang.StringUtils.defaultIfEmpty; doesn't exist i replace it by import static org.apache.commons.lang3.StringUtils.defaultIfEmpty; i think we use apache common 3", "bodyHTML": "<p dir=\"auto\">i checkout you branch and i get an error compilation here, pehaps i miss something, but your static import<br>\n<code>import static org.apache.commons.lang.StringUtils.defaultIfEmpty;</code> doesn't exist i replace it by <code>import static org.apache.commons.lang3.StringUtils.defaultIfEmpty;</code> i think we use apache common 3</p>", "author": "RSalem07", "createdAt": "2020-02-14T09:06:10Z", "path": "addons/nuxeo-core-binarymanager-cloud/nuxeo-core-binarymanager-s3/src/main/java/org/nuxeo/ecm/blob/s3/S3BlobProvider.java", "diffHunk": "@@ -179,4 +189,45 @@ protected String getContentDispositionHeader(Blob blob) {\n         return RFC2231.encodeContentDisposition(blob.getFilename(), false, null);\n     }\n \n+    @Override\n+    public BlobStatus getStatus(ManagedBlob blob) throws IOException {\n+        String key = stripBlobKeyPrefix(blob.getKey());\n+        String objectKey;\n+        String versionId;\n+        int seppos = key.indexOf(VER_SEP);\n+        if (seppos < 0) {\n+            objectKey = key;\n+            versionId = null;\n+        } else {\n+            objectKey = key.substring(0, seppos);\n+            versionId = key.substring(seppos + 1);\n+        }\n+        String bucketKey = config.bucketPrefix + objectKey;\n+        GetObjectMetadataRequest request = new GetObjectMetadataRequest(config.bucketName, bucketKey);\n+        request.setVersionId(versionId);\n+        ObjectMetadata metadata;\n+        try {\n+            metadata = config.amazonS3.getObjectMetadata(request);\n+        } catch (AmazonServiceException e) {\n+            if (S3BlobStore.isMissingKey(e)) {\n+                // don't crash for a missing blob, even though it means the storage is corrupted\n+                log.error(\"Failed to get information on blob: \" + key, e);\n+                return new BlobStatus();\n+            }\n+            throw e;\n+        }\n+        // storage class is null for STANDARD\n+        String storageClass = defaultIfEmpty(metadata.getStorageClass(), StorageClass.Standard.toString());\n+        // x-amz-restore absent", "originalCommit": "55b02cbd4f5bd050b6f645ef138b36d5ad5bb0c2", "replyToReviewId": null, "replies": [{"id": "MDI0OlB1bGxSZXF1ZXN0UmV2aWV3Q29tbWVudDM3OTU0ODEwNA==", "url": "https://github.com/nuxeo/nuxeo/pull/3741#discussion_r379548104", "bodyText": "Thanks. I intended to use commons-lang3 but I still have indirect dependencies to commons-lang somewhere.", "author": "efge", "createdAt": "2020-02-14T17:14:36Z", "replyToReviewId": "MDI0OlB1bGxSZXF1ZXN0UmV2aWV3Q29tbWVudDM3OTMxNzYyNg=="}], "type": "inlineReview"}, {"id": "MDI0OlB1bGxSZXF1ZXN0UmV2aWV3Q29tbWVudDM3OTMyNTU4OA==", "url": "https://github.com/nuxeo/nuxeo/pull/3741#discussion_r379325588", "body": "can be replaced by log.error(\"Failed to get information on blob: {}\" , key, e); but i think you main logging message are doing using the concatenation string message (i mean on others code part like [here](https://github.com/nuxeo/nuxeo/blob/feature-NXP-28417-glacier-restore-and-status-10.10/addons/nuxeo-core-binarymanager-cloud/nuxeo-core-binarymanager-s3/src/main/java/org/nuxeo/ecm/blob/s3/S3BlobProvider.java#L76) perhaps we should keep it as it is to more consistency", "bodyText": "can be replaced by log.error(\"Failed to get information on blob: {}\" , key, e); but i think you main logging message are doing using the concatenation string message (i mean on others code part like here perhaps we should keep it as it is to more consistency", "bodyHTML": "<p dir=\"auto\">can be replaced by log.error(\"Failed to get information on blob: {}\" , key, e); but i think you main logging message are doing using the concatenation string message (i mean on others code part like <a href=\"https://github.com/nuxeo/nuxeo/blob/feature-NXP-28417-glacier-restore-and-status-10.10/addons/nuxeo-core-binarymanager-cloud/nuxeo-core-binarymanager-s3/src/main/java/org/nuxeo/ecm/blob/s3/S3BlobProvider.java#L76\">here</a> perhaps we should keep it as it is to more consistency</p>", "author": "RSalem07", "createdAt": "2020-02-14T09:24:17Z", "path": "addons/nuxeo-core-binarymanager-cloud/nuxeo-core-binarymanager-s3/src/main/java/org/nuxeo/ecm/blob/s3/S3BlobProvider.java", "diffHunk": "@@ -179,4 +189,45 @@ protected String getContentDispositionHeader(Blob blob) {\n         return RFC2231.encodeContentDisposition(blob.getFilename(), false, null);\n     }\n \n+    @Override\n+    public BlobStatus getStatus(ManagedBlob blob) throws IOException {\n+        String key = stripBlobKeyPrefix(blob.getKey());\n+        String objectKey;\n+        String versionId;\n+        int seppos = key.indexOf(VER_SEP);\n+        if (seppos < 0) {\n+            objectKey = key;\n+            versionId = null;\n+        } else {\n+            objectKey = key.substring(0, seppos);\n+            versionId = key.substring(seppos + 1);\n+        }\n+        String bucketKey = config.bucketPrefix + objectKey;\n+        GetObjectMetadataRequest request = new GetObjectMetadataRequest(config.bucketName, bucketKey);\n+        request.setVersionId(versionId);\n+        ObjectMetadata metadata;\n+        try {\n+            metadata = config.amazonS3.getObjectMetadata(request);\n+        } catch (AmazonServiceException e) {\n+            if (S3BlobStore.isMissingKey(e)) {\n+                // don't crash for a missing blob, even though it means the storage is corrupted\n+                log.error(\"Failed to get information on blob: \" + key, e);", "originalCommit": "55b02cbd4f5bd050b6f645ef138b36d5ad5bb0c2", "replyToReviewId": null, "replies": null, "type": "inlineReview"}, {"id": "MDI0OlB1bGxSZXF1ZXN0UmV2aWV3Q29tbWVudDM3OTMyNzQ3MQ==", "url": "https://github.com/nuxeo/nuxeo/pull/3741#discussion_r379327471", "body": "should we returns directly a new instance of blob status as the default values will be: \r\n`downloadable=true` and `storageClass` is `null` which means a downloadable standard storage, and in fact we don't have our storage. perhapse should we add a code status or at least returns: \r\n```\r\n return new BlobStatus()withDownloadable(false);\r\n```", "bodyText": "should we returns directly a new instance of blob status as the default values will be:\ndownloadable=true and storageClass is null which means a downloadable standard storage, and in fact we don't have our storage. perhapse should we add a code status or at least returns:\n return new BlobStatus()withDownloadable(false);", "bodyHTML": "<p dir=\"auto\">should we returns directly a new instance of blob status as the default values will be:<br>\n<code>downloadable=true</code> and <code>storageClass</code> is <code>null</code> which means a downloadable standard storage, and in fact we don't have our storage. perhapse should we add a code status or at least returns:</p>\n<div class=\"snippet-clipboard-content position-relative overflow-auto\" data-snippet-clipboard-copy-content=\" return new BlobStatus()withDownloadable(false);\"><pre><code> return new BlobStatus()withDownloadable(false);\n</code></pre></div>", "author": "RSalem07", "createdAt": "2020-02-14T09:28:21Z", "path": "addons/nuxeo-core-binarymanager-cloud/nuxeo-core-binarymanager-s3/src/main/java/org/nuxeo/ecm/blob/s3/S3BlobProvider.java", "diffHunk": "@@ -179,4 +189,45 @@ protected String getContentDispositionHeader(Blob blob) {\n         return RFC2231.encodeContentDisposition(blob.getFilename(), false, null);\n     }\n \n+    @Override\n+    public BlobStatus getStatus(ManagedBlob blob) throws IOException {\n+        String key = stripBlobKeyPrefix(blob.getKey());\n+        String objectKey;\n+        String versionId;\n+        int seppos = key.indexOf(VER_SEP);\n+        if (seppos < 0) {\n+            objectKey = key;\n+            versionId = null;\n+        } else {\n+            objectKey = key.substring(0, seppos);\n+            versionId = key.substring(seppos + 1);\n+        }\n+        String bucketKey = config.bucketPrefix + objectKey;\n+        GetObjectMetadataRequest request = new GetObjectMetadataRequest(config.bucketName, bucketKey);\n+        request.setVersionId(versionId);\n+        ObjectMetadata metadata;\n+        try {\n+            metadata = config.amazonS3.getObjectMetadata(request);\n+        } catch (AmazonServiceException e) {\n+            if (S3BlobStore.isMissingKey(e)) {\n+                // don't crash for a missing blob, even though it means the storage is corrupted\n+                log.error(\"Failed to get information on blob: \" + key, e);\n+                return new BlobStatus();", "originalCommit": "55b02cbd4f5bd050b6f645ef138b36d5ad5bb0c2", "replyToReviewId": null, "replies": [{"id": "MDI0OlB1bGxSZXF1ZXN0UmV2aWV3Q29tbWVudDM3OTU1NzM3OQ==", "url": "https://github.com/nuxeo/nuxeo/pull/3741#discussion_r379557379", "bodyText": "Good point.", "author": "efge", "createdAt": "2020-02-14T17:36:19Z", "replyToReviewId": "MDI0OlB1bGxSZXF1ZXN0UmV2aWV3Q29tbWVudDM3OTMyNzQ3MQ=="}], "type": "inlineReview"}, {"id": "MDI0OlB1bGxSZXF1ZXN0UmV2aWV3Q29tbWVudDM3OTM0MDgwNw==", "url": "https://github.com/nuxeo/nuxeo/pull/3741#discussion_r379340807", "body": "can be replaced by:\r\n```\r\nRestoreObjectRequest request = new RestoreObjectRequest(bucketName, bucketKey, days).withVersionId(versionId);\r\n```", "bodyText": "can be replaced by:\nRestoreObjectRequest request = new RestoreObjectRequest(bucketName, bucketKey, days).withVersionId(versionId);", "bodyHTML": "<p dir=\"auto\">can be replaced by:</p>\n<div class=\"snippet-clipboard-content position-relative overflow-auto\" data-snippet-clipboard-copy-content=\"RestoreObjectRequest request = new RestoreObjectRequest(bucketName, bucketKey, days).withVersionId(versionId);\"><pre><code>RestoreObjectRequest request = new RestoreObjectRequest(bucketName, bucketKey, days).withVersionId(versionId);\n</code></pre></div>", "author": "RSalem07", "createdAt": "2020-02-14T09:55:54Z", "path": "addons/nuxeo-core-binarymanager-cloud/nuxeo-core-binarymanager-s3/src/main/java/org/nuxeo/ecm/blob/s3/S3BlobStore.java", "diffHunk": "@@ -676,6 +678,15 @@ public void writeBlobProperties(BlobUpdateContext blobUpdateContext) throws IOEx\n                 logTrace(\"rnote right: \" + status.toString());\n                 amazonS3.setObjectLegalHold(request);\n             }\n+            if (blobUpdateContext.restoreForDuration != null) {\n+                Duration duration = blobUpdateContext.restoreForDuration.duration;\n+                // round up duration to days\n+                int days = (int) duration.plusDays(1).minusSeconds(1).toDays();\n+                RestoreObjectRequest request = new RestoreObjectRequest(bucketName, bucketKey);\n+                request.setVersionId(versionId);\n+                request.setExpirationInDays(days);", "originalCommit": "55b02cbd4f5bd050b6f645ef138b36d5ad5bb0c2", "replyToReviewId": null, "replies": null, "type": "inlineReview"}, {"id": "MDI0OlB1bGxSZXF1ZXN0UmV2aWV3Q29tbWVudDM3OTM1NjA3OA==", "url": "https://github.com/nuxeo/nuxeo/pull/3741#discussion_r379356078", "body": "i am wondering if should use/explore the returned object:\r\n` RestoreObjectResult restoreObjectResult = amazonS3.restoreObjectV2(request);`\r\nmainly the use of `com.amazonaws.services.s3.model.RestoreObjectResult#restoreOutputPath`\r\n```\r\n    /**\r\n     * The path in the provided S3 output location where Select results will be restored to\r\n     */\r\n    private String restoreOutputPath;\r\n```\r\n\r\nOr we shouldn't care about it and when the content is available  call the  `org.nuxeo.ecm.core.blob.BlobProvider#getStatus` then we can make the call to `org.nuxeo.ecm.core.blob.BlobProvider#getStream`", "bodyText": "i am wondering if should use/explore the returned object:\n RestoreObjectResult restoreObjectResult = amazonS3.restoreObjectV2(request);\nmainly the use of com.amazonaws.services.s3.model.RestoreObjectResult#restoreOutputPath\n    /**\n     * The path in the provided S3 output location where Select results will be restored to\n     */\n    private String restoreOutputPath;\n\nOr we shouldn't care about it and when the content is available  call the  org.nuxeo.ecm.core.blob.BlobProvider#getStatus then we can make the call to org.nuxeo.ecm.core.blob.BlobProvider#getStream", "bodyHTML": "<p dir=\"auto\">i am wondering if should use/explore the returned object:<br>\n<code> RestoreObjectResult restoreObjectResult = amazonS3.restoreObjectV2(request);</code><br>\nmainly the use of <code>com.amazonaws.services.s3.model.RestoreObjectResult#restoreOutputPath</code></p>\n<div class=\"snippet-clipboard-content position-relative overflow-auto\" data-snippet-clipboard-copy-content=\"    /**\n     * The path in the provided S3 output location where Select results will be restored to\n     */\n    private String restoreOutputPath;\"><pre><code>    /**\n     * The path in the provided S3 output location where Select results will be restored to\n     */\n    private String restoreOutputPath;\n</code></pre></div>\n<p dir=\"auto\">Or we shouldn't care about it and when the content is available  call the  <code>org.nuxeo.ecm.core.blob.BlobProvider#getStatus</code> then we can make the call to <code>org.nuxeo.ecm.core.blob.BlobProvider#getStream</code></p>", "author": "RSalem07", "createdAt": "2020-02-14T10:26:30Z", "path": "addons/nuxeo-core-binarymanager-cloud/nuxeo-core-binarymanager-s3/src/main/java/org/nuxeo/ecm/blob/s3/S3BlobStore.java", "diffHunk": "@@ -676,6 +678,15 @@ public void writeBlobProperties(BlobUpdateContext blobUpdateContext) throws IOEx\n                 logTrace(\"rnote right: \" + status.toString());\n                 amazonS3.setObjectLegalHold(request);\n             }\n+            if (blobUpdateContext.restoreForDuration != null) {\n+                Duration duration = blobUpdateContext.restoreForDuration.duration;\n+                // round up duration to days\n+                int days = (int) duration.plusDays(1).minusSeconds(1).toDays();\n+                RestoreObjectRequest request = new RestoreObjectRequest(bucketName, bucketKey);\n+                request.setVersionId(versionId);\n+                request.setExpirationInDays(days);\n+                amazonS3.restoreObjectV2(request);\n+            }", "originalCommit": "55b02cbd4f5bd050b6f645ef138b36d5ad5bb0c2", "replyToReviewId": null, "replies": [{"id": "MDI0OlB1bGxSZXF1ZXN0UmV2aWV3Q29tbWVudDM3OTU1NDIzOQ==", "url": "https://github.com/nuxeo/nuxeo/pull/3741#discussion_r379554239", "bodyText": "restoreOutputPath is about \"select\" restores, which is something different.\nIn our case there's really nothing interesting in the result... It's just launching an async restore.", "author": "efge", "createdAt": "2020-02-14T17:28:59Z", "replyToReviewId": "MDI0OlB1bGxSZXF1ZXN0UmV2aWV3Q29tbWVudDM3OTM1NjA3OA=="}], "type": "inlineReview"}, {"id": "MDI0OlB1bGxSZXF1ZXN0UmV2aWV3Q29tbWVudDM3OTM1OTE4NA==", "url": "https://github.com/nuxeo/nuxeo/pull/3741#discussion_r379359184", "body": "what if we hide the fact that `null` is a the Standard storage as it is low level implementation detail (mainly AWS for instant), i think your idea is to use the blob status like more generic, but `...or {@code null} for the standard storage class.` is mainly an AWS implementation.\r\nor to be more generic perhaps we shouldn't give this information and replace it by more generic term `storageDetails`", "bodyText": "what if we hide the fact that null is a the Standard storage as it is low level implementation detail (mainly AWS for instant), i think your idea is to use the blob status like more generic, but ...or {@code null} for the standard storage class. is mainly an AWS implementation.\nor to be more generic perhaps we shouldn't give this information and replace it by more generic term storageDetails", "bodyHTML": "<p dir=\"auto\">what if we hide the fact that <code>null</code> is a the Standard storage as it is low level implementation detail (mainly AWS for instant), i think your idea is to use the blob status like more generic, but <code>...or {@code null} for the standard storage class.</code> is mainly an AWS implementation.<br>\nor to be more generic perhaps we shouldn't give this information and replace it by more generic term <code>storageDetails</code></p>", "author": "RSalem07", "createdAt": "2020-02-14T10:33:11Z", "path": "nuxeo-core/nuxeo-core-api/src/main/java/org/nuxeo/ecm/core/blob/BlobStatus.java", "diffHunk": "@@ -0,0 +1,74 @@\n+/*\n+ * (C) Copyright 2020 Nuxeo (http://nuxeo.com/) and others.\n+ *\n+ * Licensed under the Apache License, Version 2.0 (the \"License\");\n+ * you may not use this file except in compliance with the License.\n+ * You may obtain a copy of the License at\n+ *\n+ *     http://www.apache.org/licenses/LICENSE-2.0\n+ *\n+ * Unless required by applicable law or agreed to in writing, software\n+ * distributed under the License is distributed on an \"AS IS\" BASIS,\n+ * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.\n+ * See the License for the specific language governing permissions and\n+ * limitations under the License.\n+ *\n+ * Contributors:\n+ *     Florent Guillaume\n+ */\n+package org.nuxeo.ecm.core.blob;\n+\n+import java.time.Instant;\n+\n+/**\n+ * Status associated to a blob in storage.\n+ *\n+ * @since 11.1\n+ */\n+public class BlobStatus {\n+\n+    protected String storageClass;\n+\n+    protected boolean downloadable = true;\n+\n+    protected Instant downloadableUntil;\n+\n+    public BlobStatus withStorageClass(String storageClass) {\n+        this.storageClass = storageClass;\n+        return this;\n+    }\n+\n+    public BlobStatus withDownloadable(boolean downloadable) {\n+        this.downloadable = downloadable;\n+        return this;\n+    }\n+\n+    public BlobStatus withDownloadableUntil(Instant downloadableUntil) {\n+        this.downloadableUntil = downloadableUntil;\n+        return this;\n+    }\n+\n+    /**\n+     * The storage class, or {@code null} for the standard storage class.\n+     */\n+    public String getStorageClass() {\n+        return storageClass;\n+    }", "originalCommit": "55b02cbd4f5bd050b6f645ef138b36d5ad5bb0c2", "replyToReviewId": null, "replies": [{"id": "MDI0OlB1bGxSZXF1ZXN0UmV2aWV3Q29tbWVudDM3OTU1NjIxOQ==", "url": "https://github.com/nuxeo/nuxeo/pull/3741#discussion_r379556219", "bodyText": "I'll change so that the default storage class is always represented as null, even when calling getStatus (so if the storage returns a value that corresponds to its standard storage class I'll turn it into null). That helps to build a \"standard\" availability with just new BlobStatus()", "author": "efge", "createdAt": "2020-02-14T17:33:42Z", "replyToReviewId": "MDI0OlB1bGxSZXF1ZXN0UmV2aWV3Q29tbWVudDM3OTM1OTE4NA=="}], "type": "inlineReview"}, {"oid": "c2222742416b94f8751ece3234bcc0bfd60697c3", "url": "https://github.com/nuxeo/nuxeo/commit/c2222742416b94f8751ece3234bcc0bfd60697c3", "message": "NXP-28417: S3 Glacier restore and blob status", "committedDate": "2020-02-14T17:38:15Z", "type": "commit"}, {"oid": "c2222742416b94f8751ece3234bcc0bfd60697c3", "url": "https://github.com/nuxeo/nuxeo/commit/c2222742416b94f8751ece3234bcc0bfd60697c3", "message": "NXP-28417: S3 Glacier restore and blob status", "committedDate": "2020-02-14T17:38:15Z", "type": "forcePushed"}]}